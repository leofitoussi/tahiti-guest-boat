---
status: accepted
---

# Design system tokens and single-theme UI

We are standardizing the site on a single editorial premium theme with a light-only presentation. The design system uses a light primitive token layer, semantic tokens as the public API, and a small set of component tokens only where repeated component-specific values are justified. This keeps the system scalable without adding theme or dark-mode complexity that the current project does not need.
