---
interface Props {
  id?: string;
  padding?: 'none' | 'sm' | 'md' | 'lg';
  class?: string;
}

const { id, padding = 'md', class: className } = Astro.props;

const paddingClasses = {
  none: '',
  sm: 'py-8 md:py-12',
  md: 'py-14 md:py-20',
  lg: 'py-20 md:py-28',
};
---

<section id={id} class:list={[paddingClasses[padding], className]}>
  <slot />
</section>
